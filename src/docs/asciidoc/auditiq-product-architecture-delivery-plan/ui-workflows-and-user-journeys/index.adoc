== 23. UI Workflows and User Journeys

____
*v0.2 Addition.* This section defines the primary user workflows through the AuditIQ system. Each workflow is represented as a flowchart diagram showing user actions, system actions, AI actions, decision points, and audit trail integration. These workflows serve as the foundation for screen design and front-end sprint planning.

*Color Legend:*

* *Blue* (#4A6FA5) = User actions (auditor/reviewer interactions)
* *Purple* (#7B68EE) = System actions (deterministic processing)
* *Teal* (#20B2AA) = AI actions (LLM or ML-assisted)
* *Orange* (#FF7E2D) = Decision points (routing logic)
* *Dark* (#2B3E50) = Data / configuration / annotation boxes
* *Green* (#2E8B57) = Success / confirmed states
* *Red* (#C0392B) = Error / manual entry / rejection states
* *Maroon* (#8B0000) = Terminal completion

NOTE: Simplified versions of these diagrams (unstyled) appear in Appendix G.
The detailed workflow versions below add intermediate steps, side annotations, and governance detail.
____

=== 23.1 Workflow 1: Audit Lifecycle (End-to-End)

This is the primary happy-path workflow from login to exam-ready bundle export. It represents the complete user journey through a single audit.

*Actors:* Auditor (primary), System, AI Engine *Trigger:* Auditor creates a new audit *End State:* Exam-ready bundle exported; all events immutable

*Key Steps:*

[arabic]
. *Auditor logs in* → selects or creates a new audit
. *Configure audit* — select pack template, define loan sample, date range, regulatory scope
. *Upload documents* — Closing Disclosures, 1003s, appraisals, title documents
. *Docling extraction + AFC scoring* — system converts documents and computes field-level confidence (Section 10.2)
. *Schema mapping* — AI suggests field mappings; reviewer validates per two-layer confidence routing (Section 10.3)
. *Rules engine scoring* — pack rules execute against extracted + mapped data (Section 10.4)
. *AI drafts findings* — LLM generates narratives with citations (Section 10.7)
. *Reviewer effective challenge* — approve, edit, or reject each finding (Section 22)
. *Finalize audit* — lock scorecard, sign off
. *Export exam-ready bundle* — structured ZIP with manifest, PDF reports, evidence, audit trail (Section 21)

*Audit Trail:* Every step from extraction through finalization emits an immutable audit event (RFC 8785 + hash chain, Section 10.8).

[source,mermaid]
----
flowchart TD
    A([Auditor Logs In]) --> B[Create New Audit<br/>Select Pack Template]
    B --> C[Configure Audit Parameters<br/>Loan Sample, Date Range]
    C --> D[Upload Loan Documents<br/>CD, 1003, Appraisal, Title]
    D --> E[Docling Extraction<br/>+ AFC Scoring]
    E --> F[Schema Mapping<br/>AI Suggestions + Review]
    F --> G[Rules Engine Scoring<br/>Pack Rules × Extracted Data]
    G --> H[AI Generates<br/>Draft Findings]
    H --> I[Reviewer: Effective Challenge<br/>Approve / Edit / Reject]
    I --> J{All Findings<br/>Reviewed?}
    J -->|No — next finding| I
    J -->|Yes| K[Finalize Audit<br/>Lock + Sign Off]
    K --> L[Export Exam-Ready<br/>Bundle — Section 21]
    L --> M([Audit Complete])

    %% Audit Trail annotation (right side)
    N[Audit Trail:<br/>All Events Immutable<br/>RFC 8785 + Hash Chain]
    E -.-> N
    F -.-> N
    G -.-> N
    H -.-> N
    I -.-> N
    K -.-> N
    L -.-> N

    style A fill:#2E8B57,color:#fff,stroke:#1B5E3B
    style B fill:#4A6FA5,color:#fff,stroke:#2B4570
    style C fill:#4A6FA5,color:#fff,stroke:#2B4570
    style D fill:#4A6FA5,color:#fff,stroke:#2B4570
    style E fill:#7B68EE,color:#fff,stroke:#5B4CB8
    style F fill:#20B2AA,color:#fff,stroke:#178A83
    style G fill:#7B68EE,color:#fff,stroke:#5B4CB8
    style H fill:#20B2AA,color:#fff,stroke:#178A83
    style I fill:#4A6FA5,color:#fff,stroke:#2B4570
    style J fill:#FF7E2D,color:#fff,stroke:#CC5500
    style K fill:#4A6FA5,color:#fff,stroke:#2B4570
    style L fill:#3B4F6B,color:#fff,stroke:#2B3E50
    style M fill:#8B0000,color:#fff,stroke:#660000
    style N fill:#FF7E2D,color:#fff,stroke:#CC5500
----

'''''

=== 23.2 Workflow 2: Document Upload & Extraction (Docling + AFC)

This workflow details the document processing pipeline from upload through field-level confidence routing.

*Actors:* Auditor (upload), System (Docling, OCR, AFC), Reviewer (validation) *Trigger:* Auditor uploads one or more loan documents *End State:* All extracted fields confirmed (auto, reviewed, or manual) and ready for schema mapping

*Key Steps:*

[arabic]
. *Upload* — drag-and-drop or batch upload of loan documents (PDF, TIFF, scanned images)
. *Document classification* — system identifies document type (CD, 1003, Appraisal, Title, Other)
. *Docling conversion* — PDF → structured JSON using Docling layout understanding engine
. *OCR processing* — `docling-tesseract` (Apache 2.0) extracts text from scanned/image-based regions
. *AFC score computation* — 5 weighted signals combined into composite score per field:
* `docling_page_grade` (0.15) — page-level conversion quality
* `ocr_segment_confidence` (0.25) — OCR engine’s own confidence
* `pattern_validity` (0.25) — regex/format validation
* `layout_anchor_consistency` (0.20) — positional stability vs. template
* `cross_doc_redundancy` (0.15) — cross-document corroboration
. *Confidence routing:*
* *AFC ≥ 0.85* → auto-confirm (no human review needed)
* *0.50 ≤ AFC < 0.85* → human review queue (reviewer validates or overrides)
* *AFC < 0.50* → manual entry required (value too unreliable)
. *Reviewer validates* — for human-review fields, reviewer sees extracted value + confidence + source highlight
. *Field confirmed* → value passes to schema mapping

*Design Reference:* Section 10.2, Decision D1 (Section 19.2)

[source,mermaid]
----
flowchart TD
    A([User Uploads Documents]) --> B[Document Classifier<br/>CD / 1003 / Appraisal / Other]
    B --> C[Docling Conversion<br/>PDF → Structured JSON]
    C --> D[OCR Layer<br/>docling-tesseract, Apache 2.0]
    D --> E[AFC Score Computation<br/>5 Weighted Signals]

    %% AFC signal weights annotation
    E -.-> AFC[docling_page_grade 0.15<br/>ocr_segment_conf 0.25<br/>pattern_validity 0.25<br/>layout_anchor 0.20<br/>cross_doc_redundancy 0.15]

    E --> F{AFC Score<br/>Routing?}
    F -->|≥ 0.85| G([Auto-Confirm<br/>AFC ≥ 0.85])
    F -->|0.50 – 0.85| H[Human Review Queue<br/>0.50 ≤ AFC < 0.85]
    F -->|< 0.50| I[Manual Entry Required<br/>AFC < 0.50]

    H --> J[Reviewer Validates<br/>Extracted Value]
    I --> K[Reviewer Enters<br/>Correct Value]
    J --> L{Accept<br/>or Override?}
    L -->|Accept| M
    L -->|Override| K
    K --> M([Field Value Confirmed<br/>→ Schema Mapping])
    G --> M

    %% Cloud OCR fallback
    C -->|Page-grade POOR/FAIR| N[Cloud OCR Fallback]
    N --> E

    style A fill:#7B68EE,color:#fff,stroke:#5B4CB8
    style B fill:#4A6FA5,color:#fff,stroke:#2B4570
    style C fill:#7B68EE,color:#fff,stroke:#5B4CB8
    style D fill:#7B68EE,color:#fff,stroke:#5B4CB8
    style E fill:#7B68EE,color:#fff,stroke:#5B4CB8
    style AFC fill:#2B3E50,color:#fff,stroke:#1A2A3A
    style F fill:#FF7E2D,color:#fff,stroke:#CC5500
    style G fill:#2E8B57,color:#fff,stroke:#1B5E3B
    style H fill:#FF7E2D,color:#fff,stroke:#CC5500
    style I fill:#C0392B,color:#fff,stroke:#922B21
    style J fill:#4A6FA5,color:#fff,stroke:#2B4570
    style K fill:#4A6FA5,color:#fff,stroke:#2B4570
    style L fill:#FF7E2D,color:#fff,stroke:#CC5500
    style M fill:#2E8B57,color:#fff,stroke:#1B5E3B
    style N fill:#4A6FA5,color:#fff,stroke:#2B4570
----

'''''

=== 23.3 Workflow 3: Schema Mapping Review (Two-Layer Confidence)

This workflow details how extracted fields are mapped to the HMDA/MISMO target schema with AI assistance and human oversight.

*Actors:* System (matcher), AI (calibration), Reviewer (approval) *Trigger:* Extracted fields arrive from Document Processing Service *End State:* All fields mapped to target schema; approved mappings feed into rules engine

*Key Steps:*

[arabic]
. *Extracted fields arrive* — each field carries its AFC score and source document type
. *Hybrid heuristic matcher* — computes mapping suggestions using 5 weighted signals:
* `name_similarity` (0.30) — Jaro-Winkler or Levenshtein distance
* `synonym_match` (0.25) — lookup against curated synonym dictionary
* `type_compatibility` (0.20) — data type agreement
* `document_section` (0.15) — expected location in document
* `historical_mapping` (0.10) — previously approved mappings for this customer
. *Layer 1 raw signals* — `conf_extraction`, `conf_mapping`, `conf_rule` computed independently
. *Layer 2 calibrated score* — temperature scaling or isotonic regression combines Layer 1 into a single calibrated decision score
. *Routing decision:*
* *Calibrated ≥ 0.95* → auto-accept (mapping locked without review)
* *0.70 ≤ calibrated < 0.95* → human review (reviewer sees suggestion + alternatives)
* *Calibrated < 0.70* → manual entry (no reliable suggestion)
. *Human review UI* — reviewer sees:
* Suggested target field with confidence badge (green/yellow/red)
* Matching pattern type (exact, abbreviation, synonym, derived, multi-source)
* Alternative suggestions ranked by score
* Source document coverage indicator (CD-supported vs. requires other docs)
. *Reviewer action:*
* *Accept* (~10s per field) — confirm suggestion
* *Override* (~45s per field) — select alternative or type different mapping
* *Manual entry* — type target field name directly
. *Synonym dictionary update* — overrides optionally feed back to improve future matching
. *Mapping approved* → field proceeds to rules engine scoring

*Coverage Note:* With CD-only documents, expect ~10-20% of HMDA fields at ≥0.85 confidence. Full coverage requires 1003, appraisal, and title documents (Phase 2).

*Design Reference:* Section 10.3, Decision D2 (Section 19.3)

[source,mermaid]
----
flowchart TD
    A([Extracted Fields<br/>from Docling + AFC]) --> B[Hybrid Heuristic Matcher<br/>5 Weighted Signals]
    B --> C[Layer 1: Raw Signals<br/>conf_extraction, conf_mapping, conf_rule]

    %% Coverage note annotation
    C -.-> COV[Coverage Note:<br/>CD-only = 10–20% at ≥ 0.85<br/>Full coverage requires<br/>1003 + Appraisal + Title]

    C --> D[Layer 2: Calibrated Score<br/>Temperature Scaling /<br/>Isotonic Regression]
    D --> E{Routing<br/>Decision?}
    E -->|≥ 0.95| F([Auto-Accept<br/>calibrated ≥ 0.95])
    E -->|0.70 – 0.95| G[Human Review<br/>0.70 ≤ score < 0.95]
    E -->|< 0.70| H[Manual Entry<br/>score < 0.70]

    G --> UI[UI Shows:<br/>Suggested mapping,<br/>Confidence badge,<br/>Matching pattern,<br/>Alternatives]
    UI --> I{Reviewer<br/>Action?}
    I -->|Accept| J([Accept Suggestion<br/>~10s per field])
    I -->|Override| K[Override → Select<br/>Alternative ~45s]
    I -->|Manual| L[Manual Entry<br/>Type Field Name]

    K --> SYN{Update Synonym<br/>Dictionary?}
    SYN -->|Yes| DICT[Synonym Dictionary<br/>Updated]
    SYN -->|No| M
    DICT --> M
    J --> M
    L --> M
    H --> L
    F --> M([Mapping Approved<br/>→ Rules Engine])

    style A fill:#7B68EE,color:#fff,stroke:#5B4CB8
    style B fill:#4A6FA5,color:#fff,stroke:#2B4570
    style C fill:#2B3E50,color:#fff,stroke:#1A2A3A
    style COV fill:#2B3E50,color:#ccc,stroke:#555,stroke-dasharray:5 5
    style D fill:#2B3E50,color:#fff,stroke:#1A2A3A
    style E fill:#FF7E2D,color:#fff,stroke:#CC5500
    style F fill:#2E8B57,color:#fff,stroke:#1B5E3B
    style G fill:#20B2AA,color:#fff,stroke:#178A83
    style H fill:#C0392B,color:#fff,stroke:#922B21
    style UI fill:#4A6FA5,color:#fff,stroke:#2B4570
    style I fill:#FF7E2D,color:#fff,stroke:#CC5500
    style J fill:#2E8B57,color:#fff,stroke:#1B5E3B
    style K fill:#20B2AA,color:#fff,stroke:#178A83
    style L fill:#C0392B,color:#fff,stroke:#922B21
    style SYN fill:#FF7E2D,color:#fff,stroke:#CC5500
    style DICT fill:#4A6FA5,color:#fff,stroke:#2B4570
    style M fill:#2E8B57,color:#fff,stroke:#1B5E3B
----

'''''

=== 23.4 Workflow 4: Findings Review & Approval (AI Governance Controls)

This workflow details the human oversight process for AI-generated findings, enforcing the AI governance red lines from Section 22.

*Actors:* Rules Engine, AI (LLM), Reviewer (effective challenge) *Trigger:* Rules engine generates exceptions from scored data *End State:* Finding finalized with full provenance and effective challenge documentation

*Key Steps:*

[arabic]
. *Rules engine fires* — pack rules evaluate mapped data; exceptions identified
. *LLM drafts finding narrative* — generates human-readable description with citations to source documents
. *AI label applied* — every AI-drafted finding carries mandatory disclaimer: _``AI-Drafted — Requires Human Verification''_
. *AI provenance logged* — system records: `model_id`, `model_version`, `prompt_template_version`, `input_context_hash`, `timestamp`, `retrieval_corpus`, `token_counts`, `data_handling_flags`
. *Finding enters review queue* — sorted by severity and confidence
. *Reviewer opens finding* — sees AI label, evidence panel, source document highlights
. *Effective challenge* — reviewer must independently verify finding against source documents (SR 11-7 requirement)
. *Reviewer disposition:*
* *Approve* — confirm AI draft is accurate; finding becomes part of official record
* *Edit* — modify narrative, severity, or scope; system captures diff
* *Reject* — flag as false positive / not applicable; must provide rationale
. *Document challenge* — system captures:
* `effective_challenge_notes` — reviewer’s independent verification reasoning
* `disposition` — approve / edit / reject
* `output_diff` — if edited, what changed from AI draft
. *Audit event created* — `human_approval` event with full provenance chain
. *Finding finalized* → appears on scorecard

*RED LINES (Section 22.1.4):* - No AI output enters the official audit record without explicit human approval - AI output must never be represented as definitive conclusions - Reviewer is responsible for accuracy of any approved finding

*Required Disclaimers (Section 22.1.3):* - ``This content was drafted by an AI assistant and requires human verification before use'' - ``AI-generated content may contain inaccuracies — reviewer is responsible for verifying accuracy'' - ``This finding has been reviewed and approved by [reviewer name] on [date]''

*Design Reference:* Section 10.6 (Findings Management), Section 10.9 (Reporting), Section 22, Decision D1–D3

[source,mermaid]
----
flowchart TD
    A[Rules Engine<br/>Generates Exceptions] --> B[LLM Drafts Finding<br/>Narrative + Citations]

    %% AI Provenance annotation
    B -.-> PROV[AI Provenance Logged:<br/>model_id, prompt_version,<br/>input_hash, timestamp,<br/>retrieval_corpus]

    B --> C[AI Label Applied:<br/>AI-Drafted — Requires<br/>Human Verification]
    C --> D[Finding Enters<br/>Review Queue]
    D --> E[Reviewer Opens Finding<br/>Sees AI label + evidence]
    E --> F[Effective Challenge:<br/>Reviewer must independently<br/>verify against source docs]

    %% RED LINES annotation
    F -.-> RL[RED LINES Section 22.1.4:<br/>× No AI in official record<br/>without human approval<br/>× No representing AI output<br/>as definitive conclusions<br/>× Reviewer responsible<br/>for accuracy]

    F --> G{Reviewer<br/>Disposition?}
    G -->|Approve| H[Approve Finding<br/>Confirm AI draft is accurate]
    G -->|Edit| I[Edit Finding<br/>Modify narrative/severity]
    G -->|Reject| J[Reject Finding<br/>False positive / not applicable]

    H --> K[Document Challenge:<br/>effective_challenge_notes,<br/>disposition rationale,<br/>output_diff if edited]
    I --> K
    J --> K
    K --> L[Audit Event Created:<br/>human_approval event<br/>with full provenance]
    L --> M([Finding Finalized<br/>→ Scorecard])

    style A fill:#4A6FA5,color:#fff,stroke:#2B4570
    style B fill:#4A6FA5,color:#fff,stroke:#2B4570
    style PROV fill:#2B3E50,color:#ccc,stroke:#555,stroke-dasharray:5 5
    style C fill:#20B2AA,color:#fff,stroke:#178A83
    style D fill:#4A6FA5,color:#fff,stroke:#2B4570
    style E fill:#4A6FA5,color:#fff,stroke:#2B4570
    style F fill:#20B2AA,color:#fff,stroke:#178A83
    style RL fill:#C0392B,color:#fff,stroke:#922B21,stroke-dasharray:5 5
    style G fill:#FF7E2D,color:#fff,stroke:#CC5500
    style H fill:#2E8B57,color:#fff,stroke:#1B5E3B
    style I fill:#4A6FA5,color:#fff,stroke:#2B4570
    style J fill:#C0392B,color:#fff,stroke:#922B21
    style K fill:#2B3E50,color:#fff,stroke:#1A2A3A
    style L fill:#FF7E2D,color:#fff,stroke:#CC5500
    style M fill:#2E8B57,color:#fff,stroke:#1B5E3B
----

'''''

=== 23.5 UI Development Strategy

____
*Dual-track development:* AuditIQ follows both UI-driven and API-driven development practices. The workflows above define the user journeys; the API contracts in Section 10 define the backend interfaces. Screen designs (Phase 2 of this section) will be developed from these workflows.
____

*Phase 1 — Workflow Diagrams (this section):* - End-to-end audit lifecycle - Document extraction + AFC routing - Schema mapping review with two-layer confidence - Findings review with AI governance controls

*Phase 2 — Interactive Screen Wireframes (planned):* - Audit dashboard (create, manage, monitor audits) - Document upload + extraction review screen (AFC confidence badges, field-level review) - Schema mapping review screen (suggestion cards, confidence routing, synonym management) - Findings review + approval screen (effective challenge, AI labels, provenance panel) - Exam bundle export screen (manifest preview, format options)

*Design Principles:* - Confidence is visible everywhere — color-coded badges (green ≥0.85, orange 0.50–0.85, red <0.50) - AI provenance is always one click away — every AI-generated element links to its provenance record - Effective challenge is enforced by design — reviewer cannot approve without documenting their independent verification - Audit trail is ambient — every action generates an event; users see event count and last-event timestamp per artifact

'''''

END OF SECTIONS 13–23

_End of Document_
