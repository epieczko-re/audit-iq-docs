[[sect-concept-of-operations]]
== Concept of Operations (ConOps)

AuditIQ supports three distinct audit modes — manual, OCR-assisted, and fully automated pre-scan — because different audits demand different levels of human involvement.
A reviewer manually comparing a Closing Disclosure against a HMDA LAR record needs a different workflow than a system automatically validating 5,000 structured records against a LOS extract.
The platform adapts; the reviewer stays in control.

=== Audit Workflow State Machine

The following diagram shows the end-to-end state transitions for an audit run, from creation through finalization.

.Audit Workflow State Machine
[mermaid]
----
stateDiagram-v2
  direction TB
  [*] --> Created
  Created --> DocumentsUploaded: Upload loan file + HMDA doc
  DocumentsUploaded --> Extracted: Docling extraction complete
  Extracted --> ReviewerInProgress: Reviewer starts review
  ReviewerInProgress --> FindingsGenerated: Findings + scores created
  FindingsGenerated --> ManagerReview: Manager reviews escalations
  ManagerReview --> ManagerApproval: All critical findings approved
  ManagerApproval --> Finalized: Audit finalized + report generated
  Finalized --> [*]
  ReviewerInProgress --> ReviewerInProgress: Corrections, overrides
  ManagerReview --> FindingsGenerated: Manager requests re-review

  classDef blueState fill:#4A6FA5,color:#fff,stroke:#2B4570
  classDef greenState fill:#2E8B57,color:#fff,stroke:#1B5E3B

  class Created,DocumentsUploaded,Extracted,ReviewerInProgress blueState
  class FindingsGenerated,ManagerReview,ManagerApproval blueState
  class Finalized greenState
----

=== Audit Workflow: Manual Mode

[arabic]
. *Setup:* Admin creates an audit run, selects the audit pack (e.g., HMDA), uploads or links the target dataset.
. *Ingestion:* System validates the target dataset schema against the pack definition, flags validation errors.
. *Document linking:* Reviewer or batch process associates source documents (PDFs, images) with target records.
. *Review:* Reviewer opens a record in the side-by-side viewer: target data fields on the left, source document on the right. Reviewer compares each field, marks as confirmed/discrepancy/unable to determine.
. *Finding creation:* When a discrepancy is identified, reviewer creates a finding with: correct source value, root cause selection, severity (auto-suggested by pack rules, overridable), and comment.
. *Remediation:* Findings route to responsible parties. Corrective actions are tracked with due dates and evidence attachments.
. *Second review:* Configurable sampling of completed reviews for quality assurance.
. *Reporting:* Manager reviews dashboards, generates audit summary report, exports exam-ready bundle.

.Manual Mode Workflow
[mermaid]
----
sequenceDiagram
  participant Admin
  participant System
  participant Reviewer
  participant Manager

  Admin->>System: Create audit run + select pack
  Admin->>System: Upload target dataset
  System->>System: Validate schema against pack
  System-->>Admin: Validation result (errors/warnings)
  Admin->>System: Link source documents to records
  Reviewer->>System: Open record in side-by-side viewer
  Reviewer->>System: Compare fields (confirm / discrepancy / unable)
  Reviewer->>System: Create finding (value, root cause, severity, comment)
  System->>System: Route findings to responsible parties
  Reviewer->>System: Track remediation + evidence
  System->>System: Second review (QA sampling)
  Manager->>System: Review dashboards
  Manager->>System: Generate audit summary report
  System-->>Manager: Exam-ready bundle
----

=== Audit Workflow: OCR-Assisted Mode (Phase 1+)

Steps 1–3 as above, then:

[arabic, start=4]
. *OCR extraction:* Document processing pipeline extracts text, tables, and key-value pairs from source documents. Each extracted value carries an extraction confidence score.
. *Automated pre-scan:* Comparison engine applies pack rules to match target values against extracted source values. Generates preliminary findings with combined confidence scores (extraction × rule application).
. *Human review:* Reviewer sees pre-populated comparison results. High-confidence matches are pre-confirmed (reviewer can override). Low/medium-confidence items are flagged for manual verification. Reviewer focuses time on genuinely uncertain cases.
. Steps 6–8 as manual mode.

.OCR-Assisted Mode Workflow
[mermaid]
----
sequenceDiagram
  participant System
  participant Docling
  participant CompEngine as Comparison Engine
  participant Reviewer
  participant Manager

  System->>Docling: Send source documents
  Docling->>System: Extracted text, tables, key-value pairs + confidence scores
  System->>CompEngine: Apply pack rules (target vs. extracted source)
  CompEngine->>System: Preliminary findings + combined confidence scores
  System->>Reviewer: Pre-populated comparison results
  Note over Reviewer: High-confidence matches pre-confirmed
  Note over Reviewer: Low/medium-confidence items flagged
  Reviewer->>System: Verify flagged items + override if needed
  Reviewer->>System: Create/adjust findings
  System->>Manager: Escalated findings for review
  Manager->>System: Approve / request re-review
  Manager->>System: Generate report + exam-ready bundle
----

=== Finding Remediation Lifecycle

Once findings are generated (in any workflow mode), each finding follows a remediation lifecycle from identification through closure.
This lifecycle is orthogonal to the audit run state machine — an audit run may be in `FindingsGenerated` while individual findings are in different remediation states.

*Actors:* Rules Engine (creation), Reviewer (triage + retest), Remediation Owner (correction), Manager (escalation + closure approval)

*States:*

[arabic]
. *Identified* — Finding created by rules engine or reviewer; AI narrative drafted (if applicable)
. *Triaged* — Reviewer confirms severity and assigns remediation owner; AI-generated findings must pass effective challenge before triage
. *Remediation Assigned* — Owner notified; corrective action expected within SLA (Critical: 1 day, High: 3 days, Medium: 7 days, Low: 14 days)
. *Corrected* — Owner marks finding as corrected and attaches evidence of remediation
. *Retest Passed* — Reviewer independently verifies correction against source documents; finding cleared
. *Retest Failed* — Reviewer determines correction is insufficient; finding returns to Remediation Assigned with additional notes
. *Closed* — Manager approves closure (required for Critical/High findings); finding is finalized and immutable
. *Escalated* — Manager escalates finding (e.g., pattern detected across multiple loans); triggers additional review or regulatory notification workflow
. *Reopened* — Finding reopened via compensating event (<<sect-immutability>>); original finding record preserved; new event captures reopening rationale

*Approval gates:*

* *Triage → Remediation Assigned:* Reviewer must complete effective challenge for AI-generated findings
* *Corrected → Retest Passed:* Reviewer (different from remediation owner) must independently verify
* *Retest Passed → Closed:* Manager approval required for Critical and High severity findings
* *Closed → Reopened:* Manager or Compliance Officer only; compensating event required with documented rationale

.Finding Remediation State Machine
[mermaid]
----
stateDiagram-v2
  direction TB
  [*] --> Identified
  Identified --> Triaged: Reviewer confirms severity + assigns owner
  Triaged --> RemediationAssigned: Owner notified
  RemediationAssigned --> Corrected: Owner submits correction + evidence
  Corrected --> RetestPassed: Reviewer verifies correction
  Corrected --> RetestFailed: Reviewer rejects correction
  RetestFailed --> RemediationAssigned: Additional correction required
  RetestPassed --> Closed: Manager approves closure
  Identified --> Escalated: Manager escalates finding
  Triaged --> Escalated: Manager escalates finding
  Escalated --> Triaged: Escalation resolved
  Closed --> Reopened: Compensating event
  Reopened --> Triaged: Re-enter remediation

  classDef blueState fill:#4A6FA5,color:#fff,stroke:#2B4570
  classDef greenState fill:#2E8B57,color:#fff,stroke:#1B5E3B
  classDef orangeState fill:#FF7E2D,color:#fff,stroke:#CC5500
  classDef redState fill:#C0392B,color:#fff,stroke:#922B21

  class Identified,Triaged,RemediationAssigned,Corrected blueState
  class RetestPassed,Closed greenState
  class Escalated,Reopened orangeState
  class RetestFailed redState
----

*Audit trail:* Every state transition emits an immutable audit event with `actor_id`, `timestamp`, `disposition`, and `rationale`.
The complete finding remediation history is included in the exam-ready bundle (<<sect-exam-ready-bundle>>).

[[sect-remediation-extensibility]]
==== Platform Extensibility: Complaint Management and Beyond

The 9-state remediation lifecycle is intentionally designed as a generic workflow primitive — not an audit-specific construct.
The state machine (Identified → Triaged → Assigned → Corrected → Retested → Closed), the SLA-driven due dates, the severity classification, the evidence attachments, and the immutable event trail are structurally identical to what a complaint management system, a corrective action tracker, or a regulatory response workflow requires.

*Finding origin extensibility:* Every finding is tagged with an `origin` field indicating what triggered it (see <<sect-effective-rule-merge>>).
The MVP defines four origins: `regulatory`, `policy_tightened`, `policy_supplemental`, and `procedural_checkpoint`.
A fifth origin — `complaint` — is reserved for future complaint management integration.
This enables bi-directional linking: a finding can trigger a complaint (e.g., pattern of HMDA errors affecting borrowers suggests a fair lending issue), and a complaint can trigger an audit (e.g., customer alleges rate discrimination, triggering a targeted HMDA review using existing packs).

*Architectural implication:* Because the remediation lifecycle, audit trail, evidence store, and approval workflow are all entity-type-agnostic (see <<sect-policy-documents-table>> and the event store's extensible `entity_type` discriminator), extending to complaint management requires new UI screens and a Complaint Pack — not new backend infrastructure.
The same Workflow Engine, Event Store, and RBAC model serve both audit findings and complaint records.

=== Audit Workflow: Automated Pre-Scan (“Target-to-Source” / “Record-to-Record”)

For structured data audits (e.g., HMDA LAR against LOS database extract), the full audit can be automated:

[arabic]
. Both target and source datasets are uploaded as structured data (CSV, API).
. Schema mapping applies (auto-suggested, human-approved).
. Comparison engine runs all pack rules automatically.
. Output: full findings register with confidence scores.
. Human review is required for: all Critical/High findings, all findings below confidence threshold, all AI-generated narratives.

.Automated Pre-Scan Workflow
[mermaid]
----
sequenceDiagram
  participant Admin
  participant System
  participant CompEngine as Comparison Engine
  participant Reviewer
  participant Manager

  Admin->>System: Upload target dataset (CSV/API)
  Admin->>System: Upload source dataset (CSV/API)
  System->>System: Auto-suggest schema mapping
  Admin->>System: Approve schema mapping
  System->>CompEngine: Run all pack rules automatically
  CompEngine->>System: Full findings register + confidence scores
  System->>Reviewer: Critical/High findings for review
  System->>Reviewer: Below-threshold findings for review
  Reviewer->>System: Approve / override findings
  System->>Manager: Escalated findings
  Manager->>System: Final approval
  Manager->>System: Generate report
----

'''

[[wf-audit-dashboard]]
=== WF-01: Audit Dashboard

*Purpose:* Primary landing screen after login.
Shows all audits the user has access to, with status indicators, progress metrics, and quick actions.

*Roles:* All roles (filtered by RBAC — Reviewer sees assigned audits only; Manager sees all).

*Flowbite components:* Card (summary metrics), Table (audit list), Badge (status/severity), Button (actions), Dropdown (filters).

[plantuml]
----
@startsalt
{+
  <b>Audit Dashboard</b>
  ---
  {#
    <b>Active Audits</b> | <b>Pending Review</b> | <b>Completed</b> | <b>Total Findings</b>
    <color:RoyalBlue><b>12</b></color> | <color:Orange><b>4</b></color> | <color:Green><b>28</b></color> | <color:Gray><b>347</b></color>
  }
  ---
  { [+ Create New Audit] | ^Filter by Status: All^ | ^Pack: All^ | "Search audits...              " }
  ---
  {#
    <b>Audit Name</b> | <b>Pack</b> | <b>Status</b> | <b>Progress</b> | <b>Findings</b> | <b>Created</b> | <b>Actions</b>
    HMDA Q1 2026 Origination | hmda-origination v1.0 | <color:RoyalBlue>In Progress</color> | 67% | 23 (2 Critical) | 2026-02-15 | [View] [Resume]
    HMDA Q4 2025 Review | hmda-origination v1.0 | <color:Orange>Review Complete</color> | 100% | 18 (0 Critical) | 2026-01-10 | [View] [Finalize]
    CRA Assessment 2025 | cra-assessment v0.9 | <color:Green>Finalized</color> | 100% | 12 (1 Critical) | 2025-12-01 | [View] [Export]
    TRID Compliance Check | trid-disclosure v1.0 | <color:Gray>Created</color> | 0% | — | 2026-03-01 | [Configure] [Delete]
  }
  ---
  { Showing 1–4 of 40 audits | [< Prev] [1] [2] [3] [Next >] }
}
@endsalt
----

*Key interactions:*

* *[+ Create New Audit]* opens <<wf-audit-config, Audit Configuration>> screen
* *Status badge* colors: Created (gray), In Progress (blue), Review Complete (orange), Reporting (purple), Finalized (green)
* *Findings* column shows total count with critical count in red if >0
* *Progress* column shows percentage based on audit phase completion
* Table is sortable by all columns; filterable by status, pack, and date range

'''

[[wf-audit-config]]
=== WF-02: Audit Configuration

*Purpose:* Create and configure a new audit run.
User selects pack, defines scope, uploads target dataset, and sets review parameters.

*Roles:* Manager, Audit Lead.

*Flowbite components:* Form (inputs), Select (pack/scope), DatePicker (audit period), Slider (confidence threshold), File Upload (dataset), Button (actions).

[plantuml]
----
@startsalt
{+
  <b>Create New Audit</b>
  { Home > Audits > Create New }
  ===
  {+
    <b>Audit Details</b>
    {
      Audit Name        | "HMDA Q1 2026 Origination Review       "
      Pack Template     | ^hmda-origination v1.0              ^
      Audit Scope       | ^Origination                        ^
      Execution Mode    | (X) OCR-Assisted  () Manual  () Automated
    }
  }
  ---
  {+
    <b>Audit Period & Sample</b>
    {
      Start Date        | "2026-01-01  "
      End Date          | "2026-03-31  "
      Loan Sample Size  | "50           "
      Sampling Method   | ^Random Stratified               ^
    }
  }
  ---
  {+
    <b>Review Parameters</b>
    {
      Confidence Threshold | "0.85         " | (Auto-confirm findings above this threshold)
      Second Review %      | "15            " | (% of findings requiring independent second review)
      Escalation Rules     | [X] Auto-escalate Critical findings  [] Custom rules
    }
  }
  ---
  {+
    <b>Target Dataset</b>
    {
      Upload Method | (X) CSV Upload  () API Import  () RiskExec Import
      .
      [ Drag & drop CSV file here, or click to browse ]
      .
      Schema         | ^hmda-lar-2024                      ^
      Max file size: 100 MB
    }
  }
  ===
  { [Cancel] | . | . | . | [Save as Draft] | [Create Audit & Continue >>] }
}
@endsalt
----

*Key interactions:*

* *Pack Template* dropdown shows only packs with `status = 'approved'` from pack registry
* *Confidence Threshold* slider (0.50–1.00) controls finding auto-confirm cutoff; defaults to 0.85
* *[Create Audit & Continue >>]* validates all fields, creates audit run, navigates to <<wf-doc-upload, Document Upload>>
* Dataset upload triggers validation (schema check, record count, encoding); errors shown inline

'''

[[wf-audit-progress]]
=== WF-03: Audit Progress Monitor

*Purpose:* Real-time view of audit execution progress across all phases.
Shows document processing status, extraction completion, mapping progress, and findings generation.

*Roles:* All roles with access to the audit.

*Flowbite components:* Progress Bar, Timeline, Badge, Card, Table.

[plantuml]
----
@startsalt
{+
  <b>Audit Progress: HMDA Q1 2026 Origination</b>
  { Home > Audits > AUD-2026-001 > Progress }
  ===
  {+
    <b>Phase Progress</b>
    {#
      <b>Phase</b> | <b>Status</b> | <b>Progress</b> | <b>Details</b>
      1. Dataset Upload      | <color:Green>Complete</color>    | 100% | 50 records validated
      2. Document Upload      | <color:Green>Complete</color>    | 100% | 127 documents classified
      3. Extraction (Docling) | <color:RoyalBlue>Running</color> | 78%  | 99/127 documents processed
      4. Schema Mapping       | <color:Orange>Pending</color>    | 12%  | 6/50 records mapped
      5. Rules Engine         | <color:Gray>Not Started</color>  | 0%   | —
      6. Findings Review      | <color:Gray>Not Started</color>  | 0%   | —
      7. Finalization         | <color:Gray>Not Started</color>  | 0%   | —
    }
  }
  ---
  {+
    <b>Extraction Summary</b>
    {#
      <b>Metric</b> | <b>Count</b> | <b>%</b>
      Fields auto-confirmed (AFC >= 0.85)     | 1,247 | 72%
      Fields requiring review (0.50–0.84)     | 389   | 22%
      Fields requiring manual entry (< 0.50)  | 104   | 6%
      Cloud OCR fallbacks triggered            | 12    | —
    }
  }
  ---
  { [<< Back to Audit] | [View Extraction Queue] | [View Mapping Queue] }
}
@endsalt
----

*Key interactions:*

* Progress bars update in real-time via WebSocket or polling (30-second interval)
* Clicking a phase row drills into the detailed view for that phase
* *Extraction Summary* card shows AFC routing distribution, updated as documents process
