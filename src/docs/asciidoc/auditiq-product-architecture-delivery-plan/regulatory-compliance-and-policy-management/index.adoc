[[sect-regulatory-compliance]]
== Regulatory Compliance Hierarchy and Policy Management

Regulations do not exist in isolation.
A regulator creates a rule, then publishes guidance interpreting that rule.
The institution subject to the regulation writes internal policies and procedures to comply with the rule and its guidance.
An audit must verify compliance not merely against the raw regulation, but against the institution's own policies and procedures — because that is what examiners actually test against.

This section specifies how AuditIQ models this full hierarchy and how institution-specific policies and procedures are incorporated into the audit rule set.
For the base Audit Pack architecture, rule engine, and HMDA field coverage, see <<sect-audit-pack-spec>>.

[[sect-regulatory-compliance-hierarchy]]
=== The Compliance Hierarchy

.Regulatory Compliance Hierarchy — From Rule to Audit
[mermaid]
----
graph TD
  A["<b>Layer 1 — Regulation</b><br/>Federal rule with force of law<br/><i>e.g., 12 CFR 1003 (Reg C / HMDA)</i>"] --> B["<b>Layer 2 — Regulatory Guidance</b><br/>Interpretive guidance from the regulator<br/><i>e.g., CFPB Exam Procedures, FFIEC Guidelines</i>"]
  B --> C["<b>Layer 3 — Institution Policy</b><br/>Board-approved compliance policy<br/><i>e.g., ABC Bank HMDA Data Integrity Policy</i>"]
  C --> D["<b>Layer 4 — Institution Procedures</b><br/>Operational procedures implementing the policy<br/><i>e.g., ABC Bank HMDA Data Collection Procedures</i>"]
  D --> E["<b>Layer 5 — Audit Rule Set</b><br/>Effective rules used to perform the audit<br/><i>Regulatory Pack + Institution Policy Profile = Effective Rules</i>"]

  style A fill:#1B3A5C,color:#fff,stroke:#0F2440
  style B fill:#2B5A8C,color:#fff,stroke:#1B3A5C
  style C fill:#4A6FA5,color:#fff,stroke:#2B5A8C
  style D fill:#6A8FBF,color:#fff,stroke:#4A6FA5
  style E fill:#2E8B57,color:#fff,stroke:#1B5E3B
----

*Layer 1 — Regulation:* A federal or state rule with the force of law.
This is the regulatory floor — every institution subject to the regulation must comply.
Example: 12 CFR 1003 (Regulation C) governs HMDA data collection and reporting.

*Layer 2 — Regulatory Guidance:* Interpretive guidance published by the regulator to clarify how the rule should be implemented and examined.
Guidance does not have the force of law, but examiners use it as their playbook.
Example: CFPB HMDA Examination Procedures, FFIEC Interagency Guidelines.

*Layer 3 — Institution Policy:* The institution's board-approved compliance policy that addresses how it will comply with the regulation and guidance.
Policies are typically high-level statements of intent, risk appetite, and organizational responsibilities.
Example: "ABC Bank HMDA Data Integrity Policy" stating that all HMDA data will be validated against source documents within 30 days of closing.

*Layer 4 — Institution Procedures:* Operational procedures that implement the policy — the step-by-step instructions for staff.
Procedures are where the institution's specific thresholds, tolerances, escalation paths, and documentation requirements live.
Example: "ABC Bank HMDA Data Collection Procedures" specifying that loan amount discrepancies exceeding $25 require supervisor review (stricter than the regulatory standard).

*Layer 5 — Audit Rule Set:* The effective rules used to perform the audit.
This is the composition of the base Regulatory Pack (Layers 1–2) and the Institution Policy Profile (Layers 3–4).
The pack provides the regulatory floor; the policy profile tightens, extends, and contextualizes it for the specific institution.

=== Why Institution Policies Matter for Auditing

Examiners do not audit solely against the regulation.
They audit against the institution's own policies and procedures — and then assess whether those policies adequately implement the regulation.
This creates two distinct audit dimensions:

. *Regulatory compliance:* Does the institution's data and documentation meet the regulatory requirements?
(Answered by the base Regulatory Pack.)
. *Policy adherence:* Does the institution follow its own policies and procedures?
(Answered by the Institution Policy Profile overlay.)

An institution can be in regulatory compliance but in violation of its own policy.
An examiner will cite both.
AuditIQ must test both.

.Two Audit Dimensions — Regulatory Compliance vs. Policy Adherence
[mermaid]
----
graph LR
  subgraph "Regulatory Pack (Layers 1–2)"
    R1["Reg Rule: Loan amount must<br/>match within ±$50"]
    R2["Reg Rule: Action Taken date<br/>within 30 days of application"]
    R3["Reg Rule: Interest rate matches<br/>CD within ±0.125%"]
  end

  subgraph "Institution Policy Profile (Layers 3–4)"
    P1["Policy Override: Loan amount<br/>must match within ±$25"]
    P2["Supplemental: Rate-lock exceptions<br/>require dual sign-off"]
    P3["Procedural: QC review completed<br/>within 15 days of closing"]
  end

  subgraph "Effective Rule Set (Layer 5)"
    E1["Effective: Loan amount ±$25<br/><i>Policy tightened regulatory threshold</i>"]
    E2["Effective: Action Taken date<br/>within 30 days<br/><i>Regulatory rule (no policy override)</i>"]
    E3["Effective: Interest rate ±0.125%<br/><i>Regulatory rule (no policy override)</i>"]
    E4["Effective: Rate-lock dual sign-off<br/><i>Policy supplemental rule</i>"]
    E5["Effective: QC within 15 days<br/><i>Procedural checkpoint</i>"]
  end

  R1 --> E1
  P1 --> E1
  R2 --> E2
  R3 --> E3
  P2 --> E4
  P3 --> E5

  style R1 fill:#4A6FA5,color:#fff,stroke:#2B4570
  style R2 fill:#4A6FA5,color:#fff,stroke:#2B4570
  style R3 fill:#4A6FA5,color:#fff,stroke:#2B4570
  style P1 fill:#8B5E3C,color:#fff,stroke:#5C3A1E
  style P2 fill:#8B5E3C,color:#fff,stroke:#5C3A1E
  style P3 fill:#8B5E3C,color:#fff,stroke:#5C3A1E
  style E1 fill:#2E8B57,color:#fff,stroke:#1B5E3B
  style E2 fill:#2E8B57,color:#fff,stroke:#1B5E3B
  style E3 fill:#2E8B57,color:#fff,stroke:#1B5E3B
  style E4 fill:#2E8B57,color:#fff,stroke:#1B5E3B
  style E5 fill:#2E8B57,color:#fff,stroke:#1B5E3B
----

=== Three-Artifact Design

AuditIQ models this hierarchy with three distinct artifacts that compose at runtime:

.Three-Artifact Composition Model
[mermaid]
----
graph LR
  A["<b>Regulatory Pack</b><br/>Authored by RiskExec<br/>Regulatory floor (Layers 1–2)<br/>Immutable, versioned"] -->|Merge| C["<b>Effective Rule Set</b><br/>Computed at runtime<br/>What the audit runs against<br/>Deterministic composition"]
  B["<b>Institution Policy Profile</b><br/>AI-generated, human-approved<br/>Institution-specific (Layers 3–4)<br/>Immutable, versioned"] -->|Overlay| C

  style A fill:#4A6FA5,color:#fff,stroke:#2B4570
  style B fill:#8B5E3C,color:#fff,stroke:#5C3A1E
  style C fill:#2E8B57,color:#fff,stroke:#1B5E3B
----

*Artifact 1 — Regulatory Pack (existing):* The base pack as specified in <<sect-audit-pack-spec>>.
Contains all rules derived from the regulation and regulatory guidance.
Authored exclusively by RiskExec.
This is the regulatory floor — it cannot be weakened by any overlay.

*Artifact 2 — Institution Policy Profile (new):* A structured, versioned configuration artifact that captures the institution's specific policies and procedures for the regulation the pack covers.
Created via the AI-driven pipeline (see <<sect-ai-policy-pipeline>>) and approved by human compliance experts.
The profile can tighten, extend, and contextualize the base pack, but it cannot weaken the regulatory floor.

*Artifact 3 — Effective Rule Set (computed):* The deterministic merge of the Regulatory Pack and the Institution Policy Profile.
This is what the audit engine actually executes.
The merge is computed at audit-instance creation time and is immutable for the duration of the audit run.
See <<sect-effective-rule-merge>> for the merge logic specification.

[[sect-institution-policy-profile]]
=== Institution Policy Profile Specification

The Institution Policy Profile is a versioned JSON artifact with the following schema:

[source,json]
----
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AuditIQ Institution Policy Profile v1.0",
  "type": "object",
  "required": [
    "profile_id",
    "institution_id",
    "institution_name",
    "base_pack_id",
    "base_pack_version_constraint",
    "profile_version",
    "created_date",
    "status"
  ],
  "properties": {
    "profile_id": {
      "type": "string",
      "description": "Unique profile identifier, format: {institution_id}-{regulation}-policy-profile",
      "pattern": "^[a-z0-9-]+$"
    },
    "institution_id": {
      "type": "string",
      "description": "Unique institution identifier"
    },
    "institution_name": {
      "type": "string"
    },
    "base_pack_id": {
      "type": "string",
      "description": "The regulatory pack this profile overlays (e.g., 'hmda-origination-v1')"
    },
    "base_pack_version_constraint": {
      "type": "string",
      "description": "Semver range of compatible base pack versions (e.g., '>=1.0.0 <2.0.0')"
    },
    "profile_version": {
      "type": "string",
      "description": "Semantic versioning: MAJOR.MINOR.PATCH",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$"
    },
    "created_date": {
      "type": "string",
      "format": "date-time"
    },
    "updated_date": {
      "type": "string",
      "format": "date-time"
    },
    "status": {
      "type": "string",
      "enum": ["draft", "published", "deprecated", "retired"]
    },
    "approved_by": {
      "type": "array",
      "description": "Human approvers who reviewed and approved this profile",
      "items": {
        "type": "object",
        "properties": {
          "name": { "type": "string" },
          "role": { "type": "string" },
          "organization": { "type": "string" },
          "approval_date": { "type": "string", "format": "date-time" }
        }
      }
    },
    "policy_document_registry": {
      "type": "array",
      "description": "Institution policy and procedure documents that inform this profile",
      "items": {
        "type": "object",
        "required": ["document_id", "title", "document_type", "effective_date"],
        "properties": {
          "document_id": {
            "type": "string",
            "description": "Unique identifier for the policy document"
          },
          "title": {
            "type": "string",
            "description": "e.g., 'ABC Bank HMDA Data Integrity Policy v3.2'"
          },
          "document_type": {
            "type": "string",
            "enum": ["board_policy", "compliance_policy", "operating_procedure", "job_aid", "board_resolution", "risk_appetite_statement"],
            "description": "Classification of the document within the institution's governance framework"
          },
          "effective_date": {
            "type": "string",
            "format": "date"
          },
          "version": {
            "type": "string"
          },
          "owner": {
            "type": "string",
            "description": "Department or individual responsible for maintaining the document"
          },
          "storage_reference": {
            "type": "string",
            "description": "URI or reference to the document in the institution's document store"
          },
          "regulations_addressed": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Regulations this document addresses, e.g., ['12 CFR 1003', 'Reg C']"
          },
          "ai_extraction_confidence": {
            "type": "number",
            "minimum": 0,
            "maximum": 1,
            "description": "Overall confidence score from AI extraction of this document"
          }
        }
      }
    },
    "threshold_overrides": {
      "type": "array",
      "description": "Institution-specific threshold tightenings applied to base pack rules",
      "items": {
        "type": "object",
        "required": ["override_id", "base_rule_id", "override_type", "policy_citation"],
        "properties": {
          "override_id": { "type": "string" },
          "base_rule_id": {
            "type": "string",
            "description": "The rule_id in the base pack that this override tightens"
          },
          "override_type": {
            "type": "string",
            "enum": ["tolerance_tighten", "severity_escalate", "confidence_raise"],
            "description": "Type of override — all overrides must be stricter than the base rule"
          },
          "original_value": {
            "type": "number",
            "description": "The base pack's threshold value (for audit trail)"
          },
          "override_value": {
            "type": "number",
            "description": "The institution's stricter threshold value"
          },
          "policy_citation": {
            "type": "object",
            "required": ["document_id", "section"],
            "properties": {
              "document_id": { "type": "string" },
              "section": { "type": "string" },
              "page": { "type": "string" },
              "quote": {
                "type": "string",
                "description": "Verbatim quote from the policy document supporting this override"
              }
            }
          },
          "ai_suggested": {
            "type": "boolean",
            "description": "Whether this override was suggested by AI during profile creation"
          },
          "approved": {
            "type": "boolean",
            "description": "Whether a human compliance expert has approved this override"
          }
        }
      }
    },
    "supplemental_rules": {
      "type": "array",
      "description": "Additional audit rules driven by institution policy that go beyond the base regulatory pack",
      "items": {
        "type": "object",
        "required": ["rule_id", "rule_name", "rule_type", "policy_citation"],
        "properties": {
          "rule_id": {
            "type": "string",
            "description": "Prefixed with 'policy-' to distinguish from regulatory rules"
          },
          "rule_name": { "type": "string" },
          "rule_type": {
            "type": "string",
            "enum": ["documentation_check", "approval_verification", "timing_requirement", "escalation_trigger", "data_quality_check", "custom"],
            "description": "Category of the supplemental rule"
          },
          "description": { "type": "string" },
          "comparison_type": {
            "type": "string",
            "enum": ["exact_match", "tolerance_absolute", "tolerance_percentage", "temporal", "expression", "document_presence", "approval_chain"],
            "description": "Extends base comparison types with policy-specific types"
          },
          "expression": {
            "type": "string",
            "description": "CEL expression for complex policy logic"
          },
          "severity": {
            "type": "string",
            "enum": ["critical", "high", "medium", "low", "informational"]
          },
          "policy_citation": {
            "type": "object",
            "required": ["document_id", "section"],
            "properties": {
              "document_id": { "type": "string" },
              "section": { "type": "string" },
              "page": { "type": "string" },
              "quote": { "type": "string" }
            }
          },
          "ai_suggested": { "type": "boolean" },
          "approved": { "type": "boolean" }
        }
      }
    },
    "procedural_checkpoints": {
      "type": "array",
      "description": "Documentation and procedural requirements from the institution's operating procedures",
      "items": {
        "type": "object",
        "required": ["checkpoint_id", "checkpoint_name", "checkpoint_type", "policy_citation"],
        "properties": {
          "checkpoint_id": { "type": "string" },
          "checkpoint_name": { "type": "string" },
          "checkpoint_type": {
            "type": "string",
            "enum": ["document_present", "signature_required", "approval_required", "timing_deadline", "review_completed", "exception_documented"],
            "description": "Category of the procedural checkpoint"
          },
          "description": { "type": "string" },
          "verification_method": {
            "type": "string",
            "enum": ["ai_document_detection", "field_check", "manual_attestation"],
            "description": "How this checkpoint is verified during the audit"
          },
          "deadline_expression": {
            "type": "string",
            "description": "CEL expression for timing-based checkpoints, e.g., 'days_between(closing_date, qc_review_date) <= 15'"
          },
          "severity": {
            "type": "string",
            "enum": ["critical", "high", "medium", "low", "informational"]
          },
          "policy_citation": {
            "type": "object",
            "required": ["document_id", "section"],
            "properties": {
              "document_id": { "type": "string" },
              "section": { "type": "string" },
              "page": { "type": "string" },
              "quote": { "type": "string" }
            }
          },
          "ai_suggested": { "type": "boolean" },
          "approved": { "type": "boolean" }
        }
      }
    },
    "policy_to_regulation_traceability": {
      "type": "array",
      "description": "Mapping of each institution policy requirement to its regulatory basis",
      "items": {
        "type": "object",
        "required": ["policy_requirement_id", "regulation_citation"],
        "properties": {
          "policy_requirement_id": {
            "type": "string",
            "description": "References an override_id, supplemental rule_id, or checkpoint_id"
          },
          "regulation_citation": {
            "type": "string",
            "description": "The regulation this policy requirement implements, e.g., '12 CFR 1003.4(a)(1)'"
          },
          "guidance_citation": {
            "type": "string",
            "description": "The guidance document this policy requirement addresses, e.g., 'CFPB HMDA Exam Procedures, p. 47'"
          },
          "traceability_type": {
            "type": "string",
            "enum": ["direct_implementation", "stricter_than_required", "beyond_regulatory_scope", "best_practice"],
            "description": "How the policy requirement relates to the regulation"
          }
        }
      }
    },
    "gap_analysis": {
      "type": "array",
      "description": "Regulatory requirements in the base pack for which no corresponding institution policy was found via structural graph traversal or vector fallback",
      "items": {
        "type": "object",
        "properties": {
          "base_rule_id": {
            "type": "string",
            "description": "The base pack rule with no policy coverage"
          },
          "gap_type": {
            "type": "string",
            "enum": ["no_policy_found", "structurally_unanchored", "policy_weaker_than_regulation", "policy_ambiguous"],
            "description": "Nature of the gap. 'structurally_unanchored' means the regulation node exists in the graph but no policy sentences anchor to any concept in its subgraph — evidence of absence, not absence of evidence"
          },
          "risk_level": {
            "type": "string",
            "enum": ["high", "medium", "low"]
          },
          "graph_traversal_path": {
            "type": "array",
            "description": "The ontology nodes inspected during structural alignment — makes the gap auditable by showing exactly which regulatory concepts were checked",
            "items": {
              "type": "object",
              "properties": {
                "node_id": {
                  "type": "string",
                  "description": "Ontology node URI (e.g., 'fibo:LoanAmount', 'finregont:§1003.4(a)(7)')"
                },
                "node_label": {
                  "type": "string",
                  "description": "Human-readable label for the node"
                },
                "hop_distance": {
                  "type": "integer",
                  "description": "Number of hops from the regulation node in the graph traversal"
                },
                "anchored_sentences_found": {
                  "type": "integer",
                  "description": "Number of PolicySentence edges found on this node (0 for a gap)"
                }
              }
            }
          },
          "vector_fallback_attempted": {
            "type": "boolean",
            "description": "Whether vector fallback (RAG) was attempted after structural traversal yielded no results"
          },
          "vector_fallback_top_score": {
            "type": "number",
            "minimum": 0,
            "maximum": 1,
            "description": "Highest semantic similarity score from vector fallback, if attempted. Scores below the alignment threshold confirm the gap"
          },
          "recommendation": {
            "type": "string",
            "description": "AI-generated recommendation referencing the specific ontology concept the institution's policy needs to address"
          },
          "acknowledged": {
            "type": "boolean",
            "description": "Whether the institution has acknowledged this gap"
          }
        }
      }
    }
  }
}
----

=== Profile Lifecycle

Institution Policy Profiles follow the same lifecycle states as packs: Draft → Published → Deprecated → Retired.

.Institution Policy Profile Lifecycle
[mermaid]
----
stateDiagram-v2
    [*] --> Draft : AI generates draft from policy documents
    Draft --> Draft : Human review / AI re-analysis
    Draft --> Published : Compliance expert approval (RiskExec + Institution)
    Published --> Deprecated : Policy updated OR base pack major version change
    Deprecated --> Draft : New version initiated
    Deprecated --> Retired : Grace period expired
    Retired --> [*]
----

*Version pinning:* Each audit run is pinned to a specific (pack_version, profile_version) pair.
If either artifact is updated, new audit runs use the new versions; existing runs are unaffected.

*Compatibility:* The `base_pack_version_constraint` field in the profile ensures that a profile is only applied to compatible pack versions.
When a new MAJOR pack version is published, existing profiles must be re-validated and potentially re-generated.

[[sect-embedding-architecture]]
=== Sentence Transformer Embedding Architecture

The AI-Driven Policy Profile Creation Pipeline (<<sect-ai-policy-pipeline>>) depends on a two-layer embedding architecture that bridges unstructured policy language and the structured compliance knowledge graph.
Neither layer works well alone: the graph tells you what concepts exist and how they relate; the embeddings tell you which sentences in messy, unstructured policy documents are _about_ those concepts.

[[sect-embedding-domain-finetuning]]
==== Domain-Fine-Tuned Sentence Transformer

All embedding operations — regulatory citation retrieval, policy sentence indexing, and policy-to-regulation alignment — share a single sentence transformer (SBERT) fine-tuned on financial regulatory language.
This model is registered once in the Model Registry (see <<sect-model-registry>>) and versioned alongside the regulatory corpus.

General-purpose embedding models (e.g., Nomic Embed, BGE-large) are insufficient for compliance text because they fail on three critical dimensions:

* *Negation sensitivity.* "Loan amount shall match" and "loan amount shall not exceed" have opposite compliance implications but high cosine similarity in general embedding space.
A fine-tuned model learns that negation inverts the compliance vector.
* *Numeric threshold context.* "$25" and "$50" are near-identical to a general model but represent materially different compliance postures.
A fine-tuned model encodes threshold magnitude as a salient feature.
* *Deontic language.* "Must," "shall," "may," and "should" carry precise legal meanings — mandatory obligation, permissive option, aspirational guidance — that general models treat as soft synonyms.
A fine-tuned model places these terms far apart in compliance embedding space.

The fine-tuning corpus and training signal are bootstrapped from the graph itself (see <<sect-training-data-bootstrap>>).

[[sect-two-layer-embeddings]]
==== Two Embedding Layers, One Graph

.Two-Layer Embedding Architecture
[mermaid]
----
graph TD
  subgraph "Layer 1 — Ontology Node Embeddings"
    A["FIBO classes<br/>FinRegOnt sections<br/>MISMO fields"] --> B["OWL2Vec* / on2vec<br/>Graph walks + label semantics"]
    B --> C["Node embedding<br/><i>Encodes graph position +<br/>textual description</i>"]
  end

  subgraph "Layer 2 — Document Sentence Embeddings"
    D["Regulation sentences<br/>Guidance sentences<br/>Policy sentences<br/>Procedure sentences"] --> E["Domain-fine-tuned<br/>Sentence Transformer (SBERT)"]
    E --> F["Sentence embedding<br/><i>Encodes compliance semantics</i>"]
  end

  C --> G["Cross-space<br/>cosine similarity"]
  F --> G
  G --> H["Graph anchoring edge<br/>PolicySentence ──implements──→ GraphNode"]

  style A fill:#4A6FA5,color:#fff,stroke:#2B4570
  style B fill:#4A6FA5,color:#fff,stroke:#2B4570
  style C fill:#4A6FA5,color:#fff,stroke:#2B4570
  style D fill:#7B3FA0,color:#fff,stroke:#5A2D78
  style E fill:#7B3FA0,color:#fff,stroke:#5A2D78
  style F fill:#7B3FA0,color:#fff,stroke:#5A2D78
  style G fill:#8B5E3C,color:#fff,stroke:#5C3A1E
  style H fill:#2E8B57,color:#fff,stroke:#1B5E3B
----

*Layer 1 — Ontology node embeddings (structural):* Each node in the unified compliance graph — every FIBO class, every FinRegOnt regulation section, every MISMO field definition — receives an embedding that encodes both its _position in the graph_ (subclass hierarchy, equivalence relations, restriction edges) and its _label and textual description_.
OWL2Vec* generates these embeddings by walking the OWL graph structure and combining structural features with label semantics.
The on2vec approach extends this by integrating GNN-based graph structure with sentence transformer text features into a single embedding.

*Layer 2 — Document sentence embeddings (textual):* Every sentence extracted from a regulation, guidance document, institution policy, or procedure is embedded using the domain-fine-tuned sentence transformer.
Embeddings are stored in Qdrant (the same vector store used by the Regulatory Citation Engine — see <<sect-component-design>>) with metadata tagging the source document, sentence position, and document type.
Sentence-level granularity is required because individual policy sentences carry distinct obligations that paragraph-level chunking would conflate.

*Cross-space anchoring:* When a policy sentence is embedded, it is not stored as a free-floating vector.
It is _anchored_ to the nearest graph node(s) via cosine similarity between the sentence embedding (Layer 2) and the ontology node embeddings (Layer 1).
Top-_k_ candidate nodes are identified by similarity, then an LLM reranker confirms the anchor ("is this sentence genuinely about this regulatory concept?").
Confirmed anchors create a graph edge: `PolicySentence ──implements──→ GraphNode`.

This anchoring is what makes the graph _useful_ rather than merely _correct_.
The graph provides the structural scaffold; the embeddings provide the glue that attaches messy, unstructured institutional language to precise regulatory concepts.

[[sect-embedding-boundary-guardrail]]
==== Design Guardrail — Embedding Boundaries

[IMPORTANT]
====
Embeddings are used _only_ at the boundary where unstructured text meets structured knowledge.

Inside the graph, relationships — subclass, equivalence, `owl:sameAs`, FinRegOnt-to-FIBO mappings — are traversed via SPARQL and graph queries, not approximated as embedding distances.
Structural relationships are _exact_; embedding similarity is _approximate_.
Substituting one for the other introduces false positives where embedding similarity says two concepts are "close" but the graph knows they are structurally unrelated.
====

[[sect-ai-policy-pipeline]]
=== AI-Driven Policy Profile Creation Pipeline

The Institution Policy Profile is created primarily by AI, with mandatory human review and approval.
This pipeline dramatically compresses what would otherwise be weeks of manual policy analysis into days.

.AI-Driven Policy Profile Creation Pipeline
[mermaid]
----
graph TD
  A["<b>Phase 1 — Document Intake</b><br/>Institution uploads policy &<br/>procedure documents"] --> B["<b>Phase 2 — AI Extraction</b><br/>AI extracts policy-level requirements<br/>(thresholds, procedures, exceptions)"]
  B --> C["<b>Phase 3 — Regulatory Alignment</b><br/>AI maps policy requirements to<br/>base pack rules using RAG"]
  C --> D["<b>Phase 4 — Draft Profile Generation</b><br/>AI assembles structured draft<br/>with confidence scores"]
  D --> E["<b>Phase 5 — Human Review</b><br/>RiskExec + Institution compliance<br/>experts review and approve"]
  E -->|Approved| F["<b>Phase 6 — Publication</b><br/>Profile published and available<br/>for audit runs"]
  E -->|Revisions needed| B

  G["<b>Ongoing — Policy Change Detection</b><br/>Institution re-uploads updated policies;<br/>AI performs diff analysis"] --> B

  style A fill:#4A6FA5,color:#fff,stroke:#2B4570
  style B fill:#7B3FA0,color:#fff,stroke:#5A2D78
  style C fill:#7B3FA0,color:#fff,stroke:#5A2D78
  style D fill:#7B3FA0,color:#fff,stroke:#5A2D78
  style E fill:#8B5E3C,color:#fff,stroke:#5C3A1E
  style F fill:#2E8B57,color:#fff,stroke:#1B5E3B
  style G fill:#4A6FA5,color:#fff,stroke:#2B4570
----

==== Phase 1 — Policy Document Intake, Classification, and Graph Anchoring

The institution uploads their compliance policy and procedure documents.
These may include:

* Board-approved compliance policies (e.g., HMDA Data Integrity Policy)
* Operating procedures (e.g., HMDA Data Collection Procedures)
* Job aids and desk references
* Board resolutions related to compliance
* Risk appetite statements
* Internal audit findings and corrective action plans

AI classifies each uploaded document using the existing document processing pipeline (Docling-Graph — see <<sect-docling-graph>>).
Classification assigns:

* *Document type:* board_policy, compliance_policy, operating_procedure, job_aid, board_resolution, risk_appetite_statement
* *Regulatory domain(s):* which regulations the document addresses (matched against the base pack's `regulation` field)
* *Effective date and version:* extracted from document metadata or cover page
* *Extraction confidence:* overall confidence score for the AI's ability to parse and extract from this document

.Policy Document Intake — Detail Flow
[mermaid]
----
graph TD
  A["Institution<br/>uploads documents"] --> B["Docling-Graph<br/>document processing"]
  B --> C["Document<br/>classification"]
  C --> D["Policy document<br/>registry created"]
  B --> E["Sentence-level<br/>extraction"]
  E --> F["Domain-fine-tuned<br/>Sentence Transformer"]
  F --> G["Sentence embeddings<br/>stored in Qdrant"]
  F --> H["Cross-space cosine sim<br/>against ontology node<br/>embeddings (Layer 1)"]
  H --> I["LLM reranker<br/>confirms anchors"]
  I --> J["Graph anchoring edges<br/>PolicySentence ──implements──→<br/>GraphNode"]

  style A fill:#4A6FA5,color:#fff,stroke:#2B4570
  style B fill:#7B3FA0,color:#fff,stroke:#5A2D78
  style C fill:#7B3FA0,color:#fff,stroke:#5A2D78
  style D fill:#2E8B57,color:#fff,stroke:#1B5E3B
  style E fill:#7B3FA0,color:#fff,stroke:#5A2D78
  style F fill:#7B3FA0,color:#fff,stroke:#5A2D78
  style G fill:#7B3FA0,color:#fff,stroke:#5A2D78
  style H fill:#8B5E3C,color:#fff,stroke:#5C3A1E
  style I fill:#8B5E3C,color:#fff,stroke:#5C3A1E
  style J fill:#2E8B57,color:#fff,stroke:#1B5E3B
----

Extracted text is segmented at sentence-level granularity — not paragraph-level — because individual policy sentences carry distinct obligations that paragraph chunking would conflate.
Each sentence is embedded using the domain-fine-tuned sentence transformer (see <<sect-embedding-domain-finetuning>>) and stored in Qdrant with metadata tagging the source document, sentence position, and document type.

Simultaneously, each sentence embedding is compared against the ontology node embeddings (Layer 1) via cross-space cosine similarity.
Top-_k_ candidate graph nodes are identified and passed through an LLM reranker to confirm the anchor.
Confirmed anchors create graph edges (`PolicySentence ──implements──→ GraphNode`), attaching the unstructured policy language to precise concepts in the compliance knowledge graph.
Sentences that do not anchor to any graph node above a configurable confidence threshold are stored in the vector index only — they remain available for RAG fallback in Phase 3 but do not participate in structural alignment.

==== Phase 2 — AI-Driven Policy Requirement Extraction

AI reads each classified policy document and extracts *policy-level requirements* — not loan-level field data.
This is a distinct extraction task from the standard document processing pipeline.

*What AI extracts from policy documents:*

[cols="30%,35%,35%",options="header"]
|===
|Requirement Type |Description |Example

|Thresholds and tolerances
|Numeric limits that are stricter than the regulatory minimum
|"Loan amount discrepancies exceeding $25 require supervisor escalation"

|Procedural requirements
|Steps or approvals mandated by the institution's procedures
|"Rate-lock exceptions require dual sign-off from originator and branch manager within 24 hours"

|Documentation requirements
|Documents or records that must be present in the loan file
|"Signed disclosure acknowledgment must be filed within 3 business days of application"

|Exception criteria
|Conditions under which standard procedures are overridden
|"Manual overrides of automated underwriting require VP-level approval and written justification"

|Internal escalation triggers
|Conditions that trigger internal review or escalation
|"Any HMDA data error rate exceeding 2% triggers a full population re-review"

|Timing and deadline requirements
|Institution-specific deadlines beyond regulatory requirements
|"QC review must be completed within 15 days of closing"

|Risk appetite parameters
|Board-level risk tolerance statements affecting audit scope
|"Zero tolerance for HMDA reporting errors on government-monitored loan types"
|===

Each extracted requirement carries:

* A source citation (document_id, section, page, verbatim quote)
* An extraction confidence score
* A preliminary classification (threshold, procedural, documentation, exception, escalation, timing)
* A *graph anchor reference* — the ontology node(s) to which the requirement's source sentence was anchored in Phase 1, with anchoring confidence scores

The graph anchor reference is critical because it links the extracted requirement to a precise concept in the compliance knowledge graph, not just to a document location.
A requirement whose source sentence anchors strongly to a known regulatory concept (e.g., `fibo:LoanAmount` linked to `finregont:§1003.4(a)(7)`) is inherently more trustworthy than one that floats unanchored in the vector store.
The anchoring confidence is factored into the alignment confidence in Phase 3.

==== Phase 3 — Regulation-to-Policy Alignment (Graph-First, Vector-Fallback)

This is the critical step where the compliance knowledge graph acts as the structural scaffold.
For each rule in the base Regulatory Pack, alignment proceeds in two passes: a structural graph traversal first, then a vector-based fallback only if the graph yields no result.

.Graph-First Policy-to-Pack Alignment
[mermaid]
----
graph TD
  subgraph "Pass 1 — Structural Graph Traversal"
    A["Base pack rule<br/>(e.g., hmda-loan-amount-tolerance)"] --> B["Resolve rule to<br/>ontology node(s)<br/><i>FinRegOnt § → FIBO concept<br/>→ MISMO field</i>"]
    B --> C["Query graph for<br/>PolicySentence edges<br/>anchored to node(s)"]
    C --> D{"Anchored policy<br/>sentences found?"}
    D -->|Yes| E["Classify alignment<br/>from graph edges"]
  end

  subgraph "Pass 2 — Vector Fallback"
    D -->|No| F["Generate semantic<br/>query from rule"]
    F --> G["RAG retrieval from<br/>policy vector store"]
    G --> H{"Policy language<br/>found?"}
    H -->|Yes| I["Classify alignment<br/>from RAG results"]
  end

  subgraph "Gap Detection"
    H -->|No| J["Confirmed gap<br/><i>Structural + semantic<br/>evidence of absence</i>"]
  end

  E --> K["Match / Tighten / Extend"]
  I --> K

  style A fill:#4A6FA5,color:#fff,stroke:#2B4570
  style B fill:#4A6FA5,color:#fff,stroke:#2B4570
  style C fill:#4A6FA5,color:#fff,stroke:#2B4570
  style D fill:#4A6FA5,color:#fff,stroke:#2B4570
  style E fill:#2E8B57,color:#fff,stroke:#1B5E3B
  style F fill:#7B3FA0,color:#fff,stroke:#5A2D78
  style G fill:#7B3FA0,color:#fff,stroke:#5A2D78
  style H fill:#7B3FA0,color:#fff,stroke:#5A2D78
  style I fill:#8B5E3C,color:#fff,stroke:#5C3A1E
  style J fill:#CC4444,color:#fff,stroke:#993333
  style K fill:#2E8B57,color:#fff,stroke:#1B5E3B
----

*Pass 1 — Structural graph traversal:* Each base pack rule maps to one or more ontology nodes via the compliance knowledge graph (e.g., rule `hmda-loan-amount-tolerance` → `finregont:§1003.4(a)(7)` → `fibo:LoanAmount` → `mismo:LoanPrincipalAmount`).
The system queries the graph for any `PolicySentence ──implements──→` edges attached to these nodes — sentences that were anchored during Phase 1 intake.
If anchored policy sentences exist, alignment classification proceeds directly from the structured relationship.
This is the preferred path because it produces _structurally grounded_ alignments, not probabilistic retrieval results.

*Pass 2 — Vector fallback:* If the graph traversal finds no anchored policy sentences for a given rule's concept nodes, the system falls back to semantic search.
A query is generated from the rule's description and regulatory citation, and RAG retrieval searches the full policy sentence vector store in Qdrant.
Results from the fallback path carry lower alignment confidence because they lack structural grounding.

*Alignment categories:*

* *Match:* The policy restates or directly implements the regulatory requirement.
No override is needed, but the policy citation is captured in the `policy_to_regulation_traceability` array for exam defensibility.
* *Tightening:* The policy imposes a stricter threshold or higher severity than the regulation requires.
A `threshold_override` entry is generated.
* *Extension:* The policy adds requirements that go beyond the scope of the base pack rule (e.g., additional approval steps, documentation checks).
A `supplemental_rule` or `procedural_checkpoint` entry is generated.
* *Gap:* Neither structural traversal nor vector fallback found corresponding policy language.
A `gap_analysis` entry is generated with the full graph path that was inspected — which regulatory concept was checked, which FIBO/MISMO nodes were traversed, and what the search yielded.
This dual-pass failure constitutes _evidence of absence_, not just _absence of evidence_, because the graph provides structural completeness: the system knows every regulatory concept that should have coverage.

[[sect-alignment-confidence]]
*Alignment confidence:* Each alignment carries a composite confidence score incorporating four signals:

[cols="30%,15%,55%",options="header"]
|===
|Signal |Weight |Description

|Graph anchor strength
|0.35
|Cosine similarity between the policy sentence embedding and the ontology node embedding at anchoring time.
Higher values indicate the sentence is semantically close to the formal concept.

|LLM reranker confirmation
|0.25
|Confidence from the LLM reranker that confirmed the anchor in Phase 1.
Measures whether the sentence genuinely addresses the regulatory concept, not just superficially resembles it.

|Structural distance
|0.20
|Inverse of the hop count between the regulation node (the pack rule's origin) and the anchored concept node in the graph.
Direct matches (zero hops) score 1.0; multi-hop traversals decay.

|Semantic similarity (RAG)
|0.20
|Cosine similarity from the vector store retrieval.
For Pass 1 (graph-resolved) alignments, this signal is computed but weighted lower.
For Pass 2 (fallback) alignments, this is the primary signal.
|===

Low-confidence alignments (composite score < 0.70) are flagged for mandatory human review in Phase 5.

Additionally, AI performs an *unanchored scan* — searching for policy sentences that were embedded but did not anchor to any graph node above the confidence threshold.
These become candidate supplemental rules with no regulatory anchor, classified as `beyond_regulatory_scope` in the traceability mapping.

==== Phase 4 — Draft Profile Generation

AI assembles the structured draft Institution Policy Profile from the Phase 2 and Phase 3 outputs:

* `policy_document_registry` — populated from Phase 1 classification
* `threshold_overrides` — generated from Phase 3 tightenings
* `supplemental_rules` — generated from Phase 2 extensions and Phase 3 unanchored findings
* `procedural_checkpoints` — generated from Phase 2 procedural/documentation/timing requirements
* `policy_to_regulation_traceability` — generated from Phase 3 alignments
* `gap_analysis` — generated from Phase 3 gaps

Every entry is tagged with `ai_suggested: true` and `approved: false`.
No AI-generated entry becomes effective until a human sets `approved: true`.

==== Phase 5 — Human Review and Approval

The draft profile is reviewed jointly by a RiskExec compliance expert and the institution's compliance officer.

.Human Review Workflow
[mermaid]
----
graph TD
  A["Draft profile<br/>presented to reviewers"] --> B["Review threshold<br/>overrides"]
  B --> C["Review supplemental<br/>rules"]
  C --> D["Review procedural<br/>checkpoints"]
  D --> E["Review gap<br/>analysis findings"]
  E --> F{"All entries<br/>reviewed?"}
  F -->|No| B
  F -->|Yes| G{"All approved<br/>or rejected?"}
  G -->|Low-confidence items remain| H["Re-submit to AI<br/>with reviewer feedback"]
  H --> B
  G -->|Yes| I["Dual approval<br/>(RiskExec + Institution)"]
  I --> J["Profile published"]

  style A fill:#4A6FA5,color:#fff,stroke:#2B4570
  style B fill:#8B5E3C,color:#fff,stroke:#5C3A1E
  style C fill:#8B5E3C,color:#fff,stroke:#5C3A1E
  style D fill:#8B5E3C,color:#fff,stroke:#5C3A1E
  style E fill:#8B5E3C,color:#fff,stroke:#5C3A1E
  style F fill:#8B5E3C,color:#fff,stroke:#5C3A1E
  style G fill:#8B5E3C,color:#fff,stroke:#5C3A1E
  style H fill:#7B3FA0,color:#fff,stroke:#5A2D78
  style I fill:#2E8B57,color:#fff,stroke:#1B5E3B
  style J fill:#2E8B57,color:#fff,stroke:#1B5E3B
----

*Review actions per entry:*

* *Accept:* Entry is correct as generated.
Reviewer sets `approved: true`.
* *Modify:* Entry is directionally correct but needs adjustment (e.g., AI extracted the wrong threshold value).
Reviewer edits the entry and sets `approved: true`.
* *Reject:* Entry is incorrect or irrelevant.
Entry is removed from the profile.
* *Escalate:* Entry requires further analysis — the reviewer is unsure whether the policy language supports the AI's interpretation.
Entry is flagged for deeper review.

*Mandatory review triggers:*

* Any entry with extraction confidence < 0.70
* Any threshold override that changes severity from informational/low to high/critical
* Any supplemental rule with `comparison_type: expression` (CEL-based)
* All gap analysis findings (gaps require institution acknowledgment)

*Dual approval requirement:* The profile requires approval from both a RiskExec compliance expert (validates regulatory accuracy) and an institution compliance officer (validates policy interpretation).
Neither party alone can publish the profile.

==== Phase 6 — Ongoing Policy Change Detection

When an institution updates its policies or procedures, the profile must be re-evaluated.

* The institution uploads the updated policy documents.
* AI performs a diff analysis against the prior version of the policy documents.
* AI identifies which profile entries are affected by the policy changes.
* A draft profile update is generated containing only the deltas — new overrides, modified thresholds, removed entries.
* The update goes through the same human review and dual approval process.
* The profile version is incremented (MINOR for additions/tightenings, MAJOR for structural changes).

*Trigger: Base pack update.*
When RiskExec publishes a new version of the base Regulatory Pack, all Institution Policy Profiles linked to that pack are automatically flagged for re-validation.
The AI pipeline re-runs Phase 3 (alignment) against the new pack rules and generates a delta report.

[[sect-training-data-bootstrap]]
=== Training Data Bootstrapping from the Graph

The domain-fine-tuned sentence transformer (see <<sect-embedding-domain-finetuning>>) requires training pairs that teach the model how compliance language clusters.
The compliance knowledge graph generates its own training signal, creating a self-improving loop.

==== Pair Generation Strategy

[cols="20%,40%,40%",options="header"]
|===
|Pair Type |Construction |Example

|Positive (same concept)
|Two sentences anchored to the same graph node — one from a regulation, one from a policy.
The model learns that these sentences, despite different surface language, address the same compliance concept.
|Regulation: "The loan amount reported shall be verified against the closing disclosure within a tolerance of $50." +
Policy: "Loan principal discrepancies exceeding $25 require supervisor escalation." +
Label: same_concept

|Negative (different concept)
|Two sentences anchored to different graph nodes.
The model learns that superficial similarity (both mention loans, both mention dates) does not imply conceptual equivalence.
|Regulation: "Action taken date must be reported within 30 calendar days." +
Policy: "QC review must be completed within 15 business days of closing." +
Label: different_concept

|Hard negative (same domain, different obligation)
|Sentences anchored to sibling nodes in the ontology (e.g., two different HMDA fields under the same CFR section).
These are the most valuable training pairs because they force the model to distinguish between closely related but distinct compliance obligations.
|Regulation: "The applicant's ethnicity shall be reported as observed or stated." +
Policy: "Demographic data collection follows applicant self-identification procedures for race." +
Label: different_concept (ethnicity ≠ race in HMDA)
|===

==== Self-Improving Loop

As more institution policy documents are processed through the pipeline, the graph accumulates more anchored sentences.
Each new anchoring event generates additional training pairs.
The fine-tuning cycle operates as follows:

. *Accumulate:* Policy documents from multiple institutions are processed; graph anchoring edges accumulate.
. *Extract pairs:* Positive pairs from same-node anchors; negative and hard-negative pairs from different-node anchors within the same regulatory domain.
. *Fine-tune:* When the training corpus reaches a minimum threshold (≥ 500 labeled pairs — aligned with the calibration thresholds in <<sect-component-design>>), the sentence transformer is fine-tuned and a new version is registered in the Model Registry.
. *Re-anchor:* Optionally, existing policy sentences are re-embedded with the improved model and re-anchored, improving alignment quality for previously processed institutions.
. *Repeat:* Each cycle produces a model that anchors more accurately, which generates higher-quality training pairs for the next cycle.

*Cold-start strategy:* Before the system has processed enough institution documents to generate 500 training pairs, the sentence transformer operates with a warm-start fine-tune on publicly available regulatory text (CFR corpus, FFIEC examination procedures, published consent orders).
This provides baseline deontic and threshold sensitivity while the institution-specific training signal accumulates.

[[sect-effective-rule-merge]]
=== Effective Rule Set — Merge Logic

The Effective Rule Set is computed deterministically at audit-instance creation time by merging the base Regulatory Pack with the Institution Policy Profile.

.Effective Rule Set Merge Algorithm
[mermaid]
----
graph TD
  A["Load base Regulatory Pack<br/>(pack_id, pack_version)"] --> B["Load Institution Policy Profile<br/>(profile_id, profile_version)"]
  B --> C["Validate profile compatibility<br/>(base_pack_version_constraint)"]
  C --> D["Apply threshold overrides<br/><i>Only tightenings allowed</i>"]
  D --> E["Inject supplemental rules<br/><i>Prefixed with 'policy-'</i>"]
  E --> F["Inject procedural checkpoints<br/><i>Verification methods assigned</i>"]
  F --> G["Tag all rules with origin<br/><i>regulatory | policy_tightened | policy_supplemental</i>"]
  G --> H["Freeze Effective Rule Set<br/><i>Immutable for audit run duration</i>"]

  style A fill:#4A6FA5,color:#fff,stroke:#2B4570
  style B fill:#8B5E3C,color:#fff,stroke:#5C3A1E
  style C fill:#4A6FA5,color:#fff,stroke:#2B4570
  style D fill:#2E8B57,color:#fff,stroke:#1B5E3B
  style E fill:#2E8B57,color:#fff,stroke:#1B5E3B
  style F fill:#2E8B57,color:#fff,stroke:#1B5E3B
  style G fill:#2E8B57,color:#fff,stroke:#1B5E3B
  style H fill:#2E8B57,color:#fff,stroke:#1B5E3B
----

*Merge rules:*

. *Threshold overrides are applied in one direction only.*
A policy profile can reduce a tolerance (e.g., $50 → $25), escalate a severity (e.g., medium → high), or raise a confidence threshold.
It cannot widen a tolerance, downgrade a severity, or lower a confidence threshold.
If a profile attempts to weaken a regulatory rule, the merge rejects the override and logs a validation error.
. *Supplemental rules are injected alongside base pack rules.*
Supplemental rule IDs are prefixed with `policy-` to distinguish them from regulatory rules in findings and reports.
Supplemental rules are executed by the same rule engine with the same confidence propagation model.
. *Procedural checkpoints are injected as a distinct rule category.*
Checkpoints may use `ai_document_detection` (the AI pipeline checks for the presence of a required document), `field_check` (deterministic field comparison), or `manual_attestation` (requires human attestation during the audit).
. *Every rule in the Effective Rule Set is tagged with its origin:*
* `regulatory` — rule from the base pack, unmodified
* `policy_tightened` — base pack rule with a policy-driven threshold override
* `policy_supplemental` — supplemental rule from the policy profile
* `procedural_checkpoint` — procedural checkpoint from the policy profile
. *The Effective Rule Set is immutable for the duration of the audit run.*
If either the pack or the profile is updated mid-audit, the running audit continues with the original Effective Rule Set.
New audit runs pick up the latest versions.

*Finding attribution:* Every finding produced by the audit engine includes an `origin` field indicating whether it was triggered by a regulatory rule, a policy-tightened rule, a policy-supplemental rule, or a procedural checkpoint.
This allows examiners to clearly distinguish regulatory violations from policy adherence issues in the exam-ready bundle.
