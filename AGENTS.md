# Repository agent instructions

- For all AsciiDoc content under `src/docs`, keep exactly one sentence per line.
- Before finishing any docs change, run `bash scripts/lint-one-sentence-per-line.sh src/docs` and resolve all violations.
